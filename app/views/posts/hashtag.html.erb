<div class="container">
  <div class="row">
    <h2>ハッシュタグ #<%= @tag.hashname %> （全<%= @post.count %>件）</h2>
  </div>
  <div class="row">
    <%= @posts.each do |post| %>
      <div class="col-lg-4">
        <%= attachment_image_tag @post, :image %>
      </div>
    <% end %>
  </div>
</div>