<% visiter = notification.visiter %>
<% post = notification.post %>

<div>
  <%= link_to user_path(noptification.visiter) do %>
    <%= attachment_image_tag visiter, :profile_image, fallback: "no_image.jpg", size: "40x40" %>
  <% end %>
  <%= notification_form(notification) %><span><%= "(#{time_ago_in_words(notification.created_at)}前" %></span>
  <br/>
  <% if !@post_comment.nil? %>
    <p class="text-center"><%= @post_comment %></p>
  <% end %>
</div>